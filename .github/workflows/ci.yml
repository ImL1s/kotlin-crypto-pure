name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  packages: read

jobs:
  test-android:
    name: Android Core Tests
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GITHUB_ACTOR: ${{ github.actor }}
    
    steps:
      - uses: actions/checkout@v4
        with:
          path: kotlin-crypto-pure
      
      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-blockchain-client
          path: kotlin-blockchain-client

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-tx-builder
          path: kotlin-tx-builder

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-address
          path: kotlin-address

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-utxo
          path: kotlin-utxo

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-solana
          path: kotlin-solana
      
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
      
      - name: Run Core Android Unit Tests
        working-directory: kotlin-crypto-pure
        run: ./gradlew :crypto-core:testDebugUnitTest --continue

  test-jvm:
    name: JVM Unit Tests (Core & CLI)
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
        with:
          path: kotlin-crypto-pure
      
      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-blockchain-client
          path: kotlin-blockchain-client

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-tx-builder
          path: kotlin-tx-builder

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-address
          path: kotlin-address

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-utxo
          path: kotlin-utxo

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-solana
          path: kotlin-solana
      
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
      
      - name: Run Core JVM Tests
        working-directory: kotlin-crypto-pure
        run: ./gradlew :crypto-core:jvmTest --continue
        
      - name: Run Demo CLI E2E Tests
        working-directory: kotlin-crypto-pure
        run: ./gradlew :demo-cli:jvmTest --continue

  build-demo-app:
    name: Demo App Build Check
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
        with:
          path: kotlin-crypto-pure
      
      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-blockchain-client
          path: kotlin-blockchain-client

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-tx-builder
          path: kotlin-tx-builder

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-address
          path: kotlin-address

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-utxo
          path: kotlin-utxo

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-solana
          path: kotlin-solana
      
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
      
      - name: Build Desktop App
        working-directory: kotlin-crypto-pure
        run: ./gradlew :demo-app:desktopJar

  build-ios:
    name: iOS Build Check
    runs-on: macos-latest
    
    steps:
      - uses: actions/checkout@v4
        with:
          path: kotlin-crypto-pure
      
      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-blockchain-client
          path: kotlin-blockchain-client

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-tx-builder
          path: kotlin-tx-builder

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-address
          path: kotlin-address

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-utxo
          path: kotlin-utxo

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-solana
          path: kotlin-solana
      
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
      
      - name: Build iOS Framework
        working-directory: kotlin-crypto-pure
        run: ./gradlew :crypto-core:compileKotlinIosArm64

  build-watchos:
    name: watchOS Build Check
    runs-on: macos-latest
    
    steps:
      - uses: actions/checkout@v4
        with:
          path: kotlin-crypto-pure
      
      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-blockchain-client
          path: kotlin-blockchain-client

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-tx-builder
          path: kotlin-tx-builder

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-address
          path: kotlin-address

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-utxo
          path: kotlin-utxo

      - uses: actions/checkout@v4
        with:
          repository: ImL1s/kotlin-solana
          path: kotlin-solana
      
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
      
      - name: Build watchOS Framework
        working-directory: kotlin-crypto-pure
        run: ./gradlew :crypto-core:compileKotlinWatchosArm64
